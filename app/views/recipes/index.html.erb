<section class="recipes_container">
  <button type="button" class="add_btn btn"><%= link_to "Add new recipe", new_recipe_path %></button>
  <button type="button" class="add_btn btn"><%= link_to "Add new food", new_food_path %></button>
  <% @recipes.each do |recipe| %>
    <h2 class="recipe_name"><%= recipe.name %></h2>
    <p class="recipe_desc"><%= truncate(recipe.description, length: 20, omission: "...") %></p> 
